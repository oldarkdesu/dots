## General

- Add Hyprland dots
- improve the dotman thingy
- improve pman

## Scripts

- git-use-id
  - make it so that git-use-id does not assume you're using github :P
  - generalize the identities (remember to also modify the gen-ssh-keys script)
- dotman
  - add the list option to list all available configs
